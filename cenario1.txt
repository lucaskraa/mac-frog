// ============================================================
// MAC FROG — AREA 1 V5
// Manguezal das Ruinas + Claustro Alagado
// Level design metroidvania: rotas altas, atalhos, travessia por lingua,
// recompensa opcional, checkpoint e arena final.
// Arte 100% desenhada por codigo: parallax, agua, ruinas, raizes,
// vegetacao, azulejos, luz, nevoa, foreground e inimigos animados.
// ============================================================

roomNames[0] = "Área 1 - Manguezal das Ruínas";
roomNames[1] = "Área 1 - Claustro Alagado";

ground.width = WORLD_WIDTH;
checkpoints[0].x = 2660;
checkpoints[0].y = 690;
checkpoints[1].x = 3520;
checkpoints[1].y = 690;

grapple.minLength = 96;
grapple.maxLength = 300;
grapple.swingAcceleration = 8.35;
grapple.maxAngularSpeed = 4.85;
grapple.damping = 0.993;

const AREA1 = {
  room0: {
    floors: [
      { x: 0,    y: 760, width: 900,  height: WORLD_HEIGHT - 760 },
      { x: 1240, y: 760, width: 860,  height: WORLD_HEIGHT - 760 },
      { x: 2460, y: 760, width: 900,  height: WORLD_HEIGHT - 760 },
      { x: 3710, y: 760, width: 1090, height: WORLD_HEIGHT - 760 }
    ],
    pits: [
      { x: 900,  width: 340, depth: 150 },
      { x: 2100, width: 360, depth: 150 },
      { x: 3360, width: 350, depth: 150 }
    ],
    platforms: [
      { x: 250,  y: 700, width: 210, height: 22, style: "stone" },
      { x: 545,  y: 640, width: 190, height: 22, style: "stone" },
      { x: 750,  y: 690, width: 135, height: 22, style: "wood" },
      { x: 1275, y: 700, width: 215, height: 22, style: "stone" },
      { x: 1505, y: 625, width: 185, height: 22, style: "stone" },
      { x: 1710, y: 540, width: 180, height: 22, style: "stone" },
      { x: 1905, y: 610, width: 165, height: 22, style: "wood" },
      { x: 2500, y: 700, width: 210, height: 22, style: "stone" },
      { x: 2715, y: 625, width: 180, height: 22, style: "stone" },
      { x: 2900, y: 535, width: 170, height: 22, style: "stone" },
      { x: 3090, y: 445, width: 165, height: 22, style: "wood" },
      { x: 3270, y: 355, width: 195, height: 22, style: "shrine" },
      { x: 3760, y: 700, width: 220, height: 22, style: "stone" },
      { x: 4000, y: 630, width: 190, height: 22, style: "stone" },
      { x: 4225, y: 555, width: 195, height: 22, style: "stone" },
      { x: 4450, y: 630, width: 200, height: 22, style: "wood" }
    ],
    walls: [
      { x: 1350, y: 590, width: 52, height: 170 },
      { x: 1825, y: 610, width: 48, height: 150 },
      { x: 2570, y: 595, width: 52, height: 165 },
      { x: 3900, y: 590, width: 50, height: 170 },
      { x: 4590, y: 620, width: 46, height: 140 }
    ],
    anchors: [
      { id: "m0_a1", x: 1070, y: 480, radius: 12, grabRange: 325, kind: "root" },
      { id: "m0_a2", x: 2280, y: 450, radius: 12, grabRange: 335, kind: "root" },
      { id: "m0_a3", x: 2865, y: 450, radius: 12, grabRange: 300, kind: "vine" },
      { id: "m0_a4", x: 3110, y: 345, radius: 12, grabRange: 295, kind: "vine" },
      { id: "m0_a5", x: 3535, y: 465, radius: 12, grabRange: 335, kind: "root" }
    ]
  },
  room1: {
    floors: [
      { x: 0,    y: 760, width: 1120, height: WORLD_HEIGHT - 760 },
      { x: 1480, y: 760, width: 1280, height: WORLD_HEIGHT - 760 },
      { x: 3120, y: 760, width: 1680, height: WORLD_HEIGHT - 760 }
    ],
    pits: [
      { x: 1120, width: 360, depth: 150 },
      { x: 2760, width: 360, depth: 150 }
    ],
    platforms: [
      { x: 280,  y: 700, width: 190, height: 22, style: "stone" },
      { x: 570,  y: 630, width: 195, height: 22, style: "stone" },
      { x: 805,  y: 550, width: 190, height: 22, style: "wood" },
      { x: 1510, y: 700, width: 210, height: 22, style: "stone" },
      { x: 1740, y: 620, width: 190, height: 22, style: "stone" },
      { x: 1960, y: 535, width: 180, height: 22, style: "stone" },
      { x: 2200, y: 455, width: 180, height: 22, style: "shrine" },
      { x: 2450, y: 625, width: 190, height: 22, style: "wood" },
      { x: 3160, y: 700, width: 210, height: 22, style: "stone" },
      { x: 3400, y: 625, width: 200, height: 22, style: "stone" },
      { x: 3660, y: 545, width: 190, height: 22, style: "stone" },
      { x: 3935, y: 630, width: 180, height: 22, style: "wood" },
      { x: 4210, y: 690, width: 420, height: 22, style: "arena" }
    ],
    walls: [
      { x: 510,  y: 580, width: 50, height: 180 },
      { x: 1630, y: 600, width: 50, height: 160 },
      { x: 2320, y: 560, width: 52, height: 200 },
      { x: 3300, y: 590, width: 52, height: 170 }
    ],
    anchors: [
      { id: "m1_a1", x: 1295, y: 455, radius: 12, grabRange: 335, kind: "chainroot" },
      { id: "m1_a2", x: 2040, y: 390, radius: 12, grabRange: 305, kind: "vine" },
      { id: "m1_a3", x: 2940, y: 455, radius: 12, grabRange: 335, kind: "chainroot" }
    ]
  }
};

function currentAreaRoom() {
  return currentRoom === 0 ? AREA1.room0 : AREA1.room1;
}

function cloneGeometry(items) {
  return items.map(o => {
    const c = { ...o };
    delete c.style;
    return c;
  });
}

function findAreaAnchor() {
  const data = currentAreaRoom();
  const origin = getTongueOrigin();
  let best = null;
  let bestScore = Infinity;
  for (const anchor of data.anchors) {
    const d = distance(origin.x, origin.y, anchor.x, anchor.y);
    if (d > anchor.grabRange) continue;
    if (!tongueLineClearToPoint(anchor.x, anchor.y)) continue;
    const direction = anchor.x >= origin.x ? 1 : -1;
    if (direction !== mac.facing && d > 180) continue;
    const score = d + (anchor.y < origin.y ? -28 : 0);
    if (score < bestScore) {
      best = anchor;
      bestScore = score;
    }
  }
  return best;
}

canGrabPoint = function() {
  if (grapple.active) return true;
  const anchor = findAreaAnchor();
  if (!anchor) return false;
  grapplePoint.x = anchor.x;
  grapplePoint.y = anchor.y;
  grapplePoint.radius = anchor.radius;
  grapplePoint.grabRange = anchor.grabRange;
  return true;
};

rebuildSolids = function() {
  solids.length = 0;
  const data = currentAreaRoom();
  solids.push(...data.floors, ...platforms, ...walls);
  if (boss.active && boss.alive) solids.push(bossGate);
};

function makeMangueEnemy(room, type, x, patrolMin, patrolMax, opts = {}) {
  const profile = {
    caranguejo: { w: 48, h: 34, hp: 78, patrol: 72, chase: 126, damage: 9 },
    cururu:     { w: 56, h: 48, hp: 130, patrol: 42, chase: 82, damage: 15 },
    perereca:   { w: 42, h: 42, hp: 92, patrol: 58, chase: 108, damage: 11 }
  }[type] || { w: 46, h: 42, hp: 90, patrol: 55, chase: 105, damage: 10 };
  return {
    room, type, spawnX: x, x, y: 0,
    surfaceY: opts.surfaceY ?? ground.y,
    width: opts.width ?? profile.w,
    height: opts.height ?? profile.h,
    maxHp: opts.maxHp ?? profile.hp,
    hp: opts.maxHp ?? profile.hp,
    alive: true,
    patrolMin, patrolMax,
    patrolSpeed: opts.patrolSpeed ?? profile.patrol,
    chaseSpeed: opts.chaseSpeed ?? profile.chase,
    detectionRange: opts.detectionRange ?? 285,
    direction: opts.direction ?? 1,
    damage: opts.damage ?? profile.damage,
    attackCooldown: opts.attackCooldown ?? 0.85,
    attackCooldownTimer: 0,
    hurtTimer: 0,
    hurtDuration: 0.12,
    animTime: 0,
    phase: opts.phase ?? 0
  };
}

const mangueEnemies = [
  makeMangueEnemy(0, "caranguejo", 600, 500, 820),
  makeMangueEnemy(0, "cururu", 1370, 1280, 1570),
  makeMangueEnemy(0, "perereca", 1740, 1690, 1880, { surfaceY: 540, detectionRange: 220 }),
  makeMangueEnemy(0, "caranguejo", 2590, 2490, 2720),
  makeMangueEnemy(0, "cururu", 2920, 2890, 3040, { surfaceY: 535, detectionRange: 220 }),
  makeMangueEnemy(0, "perereca", 4030, 3990, 4180, { surfaceY: 630, phase: 1.2 }),
  makeMangueEnemy(0, "caranguejo", 4380, 4270, 4560),
  makeMangueEnemy(1, "perereca", 620, 560, 760, { surfaceY: 630 }),
  makeMangueEnemy(1, "cururu", 1640, 1510, 1840),
  makeMangueEnemy(1, "caranguejo", 2480, 2430, 2630, { surfaceY: 625 }),
  makeMangueEnemy(1, "perereca", 3440, 3390, 3590, { surfaceY: 625, phase: 0.7 })
];

function getExtraEnemies() {
  return mangueEnemies.filter(e => e.room === currentRoom);
}

function resetMangueEnemies(room) {
  for (const e of mangueEnemies) {
    if (e.room !== room) continue;
    e.x = e.spawnX;
    e.y = e.surfaceY - e.height;
    e.hp = e.maxHp;
    e.alive = true;
    e.attackCooldownTimer = 0;
    e.hurtTimer = 0;
    e.animTime = 0;
  }
}

function updateOneMangueEnemy(e, dt) {
  if (!e.alive) return;
  e.animTime = (e.animTime || 0) + dt;
  if (e.hurtTimer > 0) e.hurtTimer -= dt;
  if (e.attackCooldownTimer > 0) e.attackCooldownTimer -= dt;

  const bob = e.type === "perereca"
    ? Math.max(0, Math.sin(e.animTime * 3.2 + (e.phase || 0))) * 7
    : 0;
  e.y = (e.surfaceY ?? ground.y) - e.height - bob;

  const ec = e.x + e.width / 2;
  const mc = mac.x + mac.width / 2;
  const dx = Math.abs(mc - ec);
  const dy = Math.abs((mac.y + mac.height / 2) - (e.y + e.height / 2));
  let speed = e.patrolSpeed;

  if (!mac.dead && dx <= e.detectionRange && dy < 100) {
    e.direction = mc < ec ? -1 : 1;
    speed = e.chaseSpeed;
  } else {
    if (e.x <= e.patrolMin) e.direction = 1;
    if (e.x + e.width >= e.patrolMax) e.direction = -1;
  }

  e.x += e.direction * speed * dt;
  e.x = Math.max(e.patrolMin, Math.min(e.x, e.patrolMax - e.width));

  if (!mac.dead &&
      rectsOverlap({ x:e.x, y:e.y, width:e.width, height:e.height }, getMacHitbox()) &&
      e.attackCooldownTimer <= 0) {
    damageMac(e.damage, e.x + e.width / 2);
    e.attackCooldownTimer = e.attackCooldown;
  }
}

updateEnemy = function(dt) {
  if (enemy.alive) {
    if (typeof enemy.type !== "string") enemy.type = "caranguejo";
    if (typeof enemy.surfaceY !== "number") enemy.surfaceY = ground.y;
    if (typeof enemy.animTime !== "number") enemy.animTime = 0;
    updateOneMangueEnemy(enemy, dt);
  }
  for (const e of getExtraEnemies()) updateOneMangueEnemy(e, dt);
};

function damageExtraEnemy(e, amount) {
  if (!e || !e.alive) return;
  e.hp -= amount;
  e.hurtTimer = e.hurtDuration;
  if (e.hp <= 0) {
    e.hp = 0;
    e.alive = false;
  }
}

function hitExtraEnemy(hitbox, amount) {
  if (!hitbox) return false;
  for (const e of getExtraEnemies()) {
    if (!e.alive) continue;
    if (rectsOverlap(hitbox, { x:e.x, y:e.y, width:e.width, height:e.height })) {
      damageExtraEnemy(e, amount);
      return true;
    }
  }
  return false;
}

function updateExtraEnemyCombat() {
  if (tongue.active && !grapple.active && !tongue.didHitEnemy && hitExtraEnemy(getTongueHitbox(), 15)) tongue.didHitEnemy = true;
  if (normalAttack.active && normalAttack.hitboxActive && !normalAttack.didHitEnemy && hitExtraEnemy(getNormalAttackHitbox(), 25)) normalAttack.didHitEnemy = true;

  for (let i = waterShots.length - 1; i >= 0; i--) {
    const s = waterShots[i];
    if (hitExtraEnemy({ x:s.x-s.radius, y:s.y-s.radius, width:s.radius*2, height:s.radius*2 }, 20)) waterShots.splice(i,1);
  }

  if (waterBurst.active && !waterBurst.didHitEnemy && hitExtraEnemy(getWaterBurstHitbox(), 35)) waterBurst.didHitEnemy = true;
  if (waterUltimate.active && !waterUltimate.didHitEnemy &&
      hitExtraEnemy({ x:waterUltimate.waveX, y:ground.y-waterUltimate.waveHeight, width:waterUltimate.waveWidth, height:waterUltimate.waveHeight }, 60)) {
    waterUltimate.didHitEnemy = true;
  }

  for (let i = windBlades.length - 1; i >= 0; i--) {
    const b = windBlades[i];
    if (hitExtraEnemy({ x:b.x, y:b.y, width:b.width, height:b.height }, 20)) windBlades.splice(i,1);
  }

  if (windGust.active && !windGust.didHitEnemy && hitExtraEnemy(getWindGustHitbox(), 35)) windGust.didHitEnemy = true;
  if (windUltimate.active && windUltimate.timer >= windUltimate.spawnDelay && !windUltimate.didHitEnemy &&
      hitExtraEnemy({ x:windUltimate.x, y:ground.y-windUltimate.height, width:windUltimate.width, height:windUltimate.height }, 70)) {
    windUltimate.didHitEnemy = true;
  }
}

function applyArea1Room(index) {
  ground.width = WORLD_WIDTH;
  const data = index === 0 ? AREA1.room0 : AREA1.room1;
  setRoomGeometry(cloneGeometry(data.platforms), cloneGeometry(data.walls));

  const firstAnchor = data.anchors[0];
  grapplePoint.x = firstAnchor.x;
  grapplePoint.y = firstAnchor.y;
  grapplePoint.radius = firstAnchor.radius;
  grapplePoint.grabRange = firstAnchor.grabRange;

  if (index === 0) {
    waterFly.baseX = 3360;
    waterFly.x = waterFly.baseX;
    waterFly.baseY = 300;
    waterFly.y = waterFly.baseY;
    waterFly.challengeTime = 0;
    waterFly.alive = !waterFly.collected;
    windFly.alive = false;

    enemy.type = "caranguejo";
    enemy.surfaceY = ground.y;
    enemy.x = 820;
    enemy.patrolMin = 720;
    enemy.patrolMax = 890;
    enemy.patrolSpeed = 62;
    enemy.chaseSpeed = 112;
    enemy.detectionRange = 260;
    enemy.damage = 10;
  } else {
    waterFly.alive = false;
    windFly.baseX = 2250;
    windFly.x = windFly.baseX;
    windFly.baseY = 410;
    windFly.y = windFly.baseY;
    windFly.challengeTime = 0;
    windFly.alive = !windFly.collected;

    enemy.type = "cururu";
    enemy.surfaceY = ground.y;
    enemy.x = 1870;
    enemy.patrolMin = 1740;
    enemy.patrolMax = 2020;
    enemy.patrolSpeed = 40;
    enemy.chaseSpeed = 82;
    enemy.detectionRange = 290;
    enemy.damage = 15;

    bossGate.x = 4040;
    bossGate.y = 520;
    boss.x = 4400;
    boss.y = ground.y - boss.height;
    boss.arenaLeft = 4090;
    boss.arenaRight = 4770;
  }

  rebuildSolids();
  resetMangueEnemies(index);
  enemy.y = enemy.surfaceY - enemy.height;
  enemy.hp = enemy.maxHp;
  enemy.alive = true;
  enemy.attackCooldownTimer = 0;
  enemy.hurtTimer = 0;
}

const __area1BaseLoadRoom = loadRoom;
loadRoom = function(index, entrySide = "left") {
  __area1BaseLoadRoom(index, entrySide);
  applyArea1Room(index);
  mac.y = ground.y - mac.height;
  resolveMacOverlaps();
  snapCameraToMac();
};

applyArea1Room(0);

const __area1BaseUpdateWaterFly = updateWaterFly;
updateWaterFly = function(dt) {
  if (currentRoom !== 0 || !waterFly.alive || typeof waterFly.baseX !== "number") {
    __area1BaseUpdateWaterFly(dt);
    return;
  }
  waterFly.animTime = (waterFly.animTime || 0) + dt;
  waterFly.challengeTime = (waterFly.challengeTime || 0) + dt;
  const t = waterFly.challengeTime;
  waterFly.x = waterFly.baseX + Math.sin(t * 1.55) * 62;
  waterFly.y = waterFly.baseY + Math.sin(t * 3.1) * 25 + Math.sin(t * 1.2) * 8;
};

const __area1BaseUpdateWindFly = updateWindFly;
updateWindFly = function(dt) {
  if (currentRoom !== 1 || !windFly.alive || typeof windFly.baseX !== "number") {
    __area1BaseUpdateWindFly(dt);
    return;
  }
  windFly.animTime = (windFly.animTime || 0) + dt;
  windFly.challengeTime = (windFly.challengeTime || 0) + dt;
  const t = windFly.challengeTime;
  windFly.x = windFly.baseX + Math.sin(t * 1.35) * 48;
  windFly.y = windFly.baseY + Math.cos(t * 2.6) * 22;
};

const __area1BaseUpdate = update;
update = function(dt) {
  __area1BaseUpdate(dt);
  updateExtraEnemyCombat();
  if (!mac.dead && mac.y > WORLD_HEIGHT + 20) killMac();
};

const AREA1_PALETTE = {
  skyTop: "#8fcde6",
  skyMid: "#dcebd4",
  skyLow: "#a4bf8e",
  farHill: "rgba(64,101,76,0.25)",
  midTree: "rgba(43,75,57,0.44)",
  stone: "#70715a",
  stoneTop: "#9a9478",
  stoneDark: "#494c3d",
  moss: "#66834d",
  mossBright: "#8da45a",
  root: "#514633",
  vine: "#52653f",
  azulejo: "#3e8790",
  azulejoLight: "#91c5be",
  wood: "#76583a",
  woodDark: "#4d3827"
};

function seededNoise(n) {
  const x = Math.sin(n * 12.9898 + 78.233) * 43758.5453;
  return x - Math.floor(x);
}

function drawCloud(x, y, scale, alpha) {
  ctx.save();
  ctx.globalAlpha = alpha;
  ctx.fillStyle = "#ffffff";
  const parts = [[0,0,36],[35,-12,30],[66,0,38],[92,8,26],[28,12,38],[64,18,40]];
  for (const [px,py,r] of parts) {
    ctx.beginPath();
    ctx.arc(x + px*scale, y + py*scale, r*scale, 0, Math.PI*2);
    ctx.fill();
  }
  ctx.restore();
}

function drawDistantChurch(baseX, baseY, alpha=0.3) {
  ctx.save();
  ctx.globalAlpha = alpha;
  ctx.fillStyle = "#737b69";
  ctx.fillRect(baseX, baseY, 120, 94);
  ctx.fillRect(baseX-24, baseY-48, 30, 142);
  ctx.fillRect(baseX+114, baseY-48, 30, 142);
  ctx.fillRect(baseX-18, baseY-60, 18, 14);
  ctx.fillRect(baseX+120, baseY-60, 18, 14);
  ctx.fillStyle = "rgba(235,238,211,0.35)";
  ctx.fillRect(baseX+48, baseY+30, 24, 64);
  ctx.restore();
}

function drawDistantArch(x, baseY, scale, alpha) {
  ctx.save();
  ctx.globalAlpha = alpha;
  ctx.fillStyle = "#5d6f5e";
  ctx.fillRect(x, baseY-120*scale, 20*scale, 120*scale);
  ctx.fillRect(x+86*scale, baseY-120*scale, 20*scale, 120*scale);
  ctx.strokeStyle = "#5d6f5e";
  ctx.lineWidth = 18*scale;
  ctx.beginPath();
  ctx.arc(x+53*scale, baseY-112*scale, 44*scale, Math.PI, Math.PI*2);
  ctx.stroke();
  ctx.restore();
}

function drawMangroveBackground() {
  const p = AREA1_PALETTE;
  const sky = ctx.createLinearGradient(0,0,0,HEIGHT);

  if (currentRoom === 0) {
    sky.addColorStop(0,p.skyTop);
    sky.addColorStop(0.45,p.skyMid);
    sky.addColorStop(1,p.skyLow);
  } else {
    sky.addColorStop(0,"#77aebd");
    sky.addColorStop(0.48,"#a9c5b0");
    sky.addColorStop(1,"#6f8d72");
  }

  ctx.fillStyle = sky;
  ctx.fillRect(0,0,WIDTH,HEIGHT);

  if (currentRoom === 0) {
    drawCloud(90 - (camera.x*0.015)%360, 95, 0.75, 0.36);
    drawCloud(620 - (camera.x*0.02)%500, 145, 0.55, 0.28);
  }

  ctx.save();
  const glow = ctx.createRadialGradient(WIDTH*0.73,105,10,WIDTH*0.73,105,150);
  glow.addColorStop(0,"rgba(255,245,190,0.34)");
  glow.addColorStop(1,"rgba(255,245,190,0)");
  ctx.fillStyle = glow;
  ctx.fillRect(0,0,WIDTH,HEIGHT);
  ctx.restore();

  const far = -((camera.x*0.055)%520);
  ctx.fillStyle = p.farHill;

  for (let i=-1;i<Math.ceil(WIDTH/360)+2;i++) {
    const x = far+i*360;
    ctx.beginPath();
    ctx.moveTo(x,HEIGHT*0.63);
    ctx.quadraticCurveTo(x+85,HEIGHT*0.31,x+190,HEIGHT*0.56);
    ctx.quadraticCurveTo(x+275,HEIGHT*0.38,x+370,HEIGHT*0.63);
    ctx.lineTo(x+370,HEIGHT*0.72);
    ctx.lineTo(x,HEIGHT*0.72);
    ctx.closePath();
    ctx.fill();
  }

  drawDistantChurch(WIDTH*0.70-(camera.x*0.035)%260, 205, currentRoom===0?0.27:0.18);

  const archOffset = -((camera.x*0.09)%480);
  for (let i=-1;i<4;i++) drawDistantArch(archOffset+i*480+170, HEIGHT*0.64, 0.8, 0.22);

  const mid = -((camera.x*0.17)%250);
  ctx.strokeStyle = p.midTree;
  ctx.lineCap = "round";

  for (let i=-1;i<Math.ceil(WIDTH/150)+2;i++) {
    const x = mid+i*150+35;
    const base = HEIGHT*0.70;
    const top = HEIGHT*(0.29+(i%4)*0.025);

    ctx.lineWidth = 8;
    ctx.beginPath();
    ctx.moveTo(x,base);
    ctx.quadraticCurveTo(x-16,(base+top)/2,x,top);
    ctx.stroke();

    ctx.lineWidth = 3.5;
    for (let a=-2;a<=2;a++) {
      ctx.beginPath();
      ctx.moveTo(x,top+8);
      ctx.quadraticCurveTo(x+a*16,top-15,x+a*34,top+10);
      ctx.stroke();
    }
  }

  const waterY = HEIGHT*0.68;
  const waterGrad = ctx.createLinearGradient(0,waterY,0,HEIGHT);
  waterGrad.addColorStop(0,"rgba(76,157,156,0.38)");
  waterGrad.addColorStop(1,"rgba(34,94,91,0.50)");
  ctx.fillStyle = waterGrad;
  ctx.fillRect(0,waterY,WIDTH,HEIGHT-waterY);

  ctx.strokeStyle = "rgba(232,251,232,0.30)";
  ctx.lineWidth = 2;

  const time = performance.now()*0.001;
  for (let y=waterY+14;y<HEIGHT;y+=24) {
    const phase = Math.sin(time*1.3+y*.03)*18;
    for (let x=-100;x<WIDTH+100;x+=120) {
      ctx.beginPath();
      ctx.moveTo(x+phase,y);
      ctx.quadraticCurveTo(x+28+phase,y-3,x+62+phase,y);
      ctx.stroke();
    }
  }

  const fog = ctx.createLinearGradient(0,HEIGHT*0.48,0,HEIGHT*0.78);
  fog.addColorStop(0,"rgba(230,242,214,0)");
  fog.addColorStop(0.55,currentRoom===0?"rgba(230,242,214,0.12)":"rgba(205,228,210,0.10)");
  fog.addColorStop(1,"rgba(230,242,214,0)");
  ctx.fillStyle = fog;
  ctx.fillRect(0,HEIGHT*0.45,WIDTH,HEIGHT*0.36);
}

function drawGroundSegment(seg) {
  const p = AREA1_PALETTE;
  const grad = ctx.createLinearGradient(0,seg.y,0,WORLD_HEIGHT);
  grad.addColorStop(0,currentRoom===0?"#65684d":"#555d4d");
  grad.addColorStop(1,currentRoom===0?"#35443a":"#303a35");
  ctx.fillStyle = grad;
  ctx.fillRect(seg.x,seg.y,seg.width,seg.height);
  ctx.fillStyle = p.mossBright;
  ctx.fillRect(seg.x,seg.y,seg.width,7);
  ctx.fillStyle = "rgba(31,57,41,0.44)";
  for (let x=seg.x+8;x<seg.x+seg.width;x+=36) ctx.fillRect(x,seg.y-4,20,5);
}

function drawWaterPit(pit) {
  const t = performance.now()*0.001;
  const y0 = ground.y;
  const grad = ctx.createLinearGradient(0,y0,0,WORLD_HEIGHT);
  grad.addColorStop(0,"#5ea9a5");
  grad.addColorStop(1,"#255f61");
  ctx.fillStyle = grad;
  ctx.fillRect(pit.x,y0,pit.width,WORLD_HEIGHT-y0);

  ctx.strokeStyle = "rgba(226,250,233,0.54)";
  ctx.lineWidth = 2;

  for (let y=y0+10;y<WORLD_HEIGHT;y+=23) {
    const phase = Math.sin(t*1.6+y*.025)*10;
    for (let x=pit.x+10;x<pit.x+pit.width-18;x+=56) {
      ctx.beginPath();
      ctx.moveTo(x+phase,y);
      ctx.quadraticCurveTo(x+16+phase,y-3,x+34+phase,y);
      ctx.stroke();
    }
  }

  ctx.strokeStyle = "#514532";
  ctx.lineWidth = 6;

  for (let i=0;i<3;i++) {
    const x = pit.x+pit.width*(.2+i*.28);
    ctx.beginPath();
    ctx.moveTo(x,y0+66);
    ctx.lineTo(x+(i%2?8:-7),y0+12);
    ctx.stroke();
  }
}

function platformStyleFor(rect) {
  const source = currentAreaRoom().platforms.find(p => p.x===rect.x && p.y===rect.y && p.width===rect.width);
  return source?.style || "stone";
}

function drawStonePlatform(rect, style="stone") {
  const p = AREA1_PALETTE;

  if (style === "wood") {
    ctx.fillStyle = p.woodDark;
    ctx.fillRect(rect.x,rect.y,rect.width,rect.height);
    ctx.fillStyle = p.wood;
    ctx.fillRect(rect.x,rect.y,rect.width,7);

    ctx.strokeStyle = "rgba(43,28,18,0.55)";
    ctx.lineWidth = 2;
    for (let x=rect.x+22;x<rect.x+rect.width;x+=42) {
      ctx.beginPath();
      ctx.moveTo(x,rect.y);
      ctx.lineTo(x-4,rect.y+rect.height);
      ctx.stroke();
    }

    ctx.strokeStyle = "#435b38";
    for (let x=rect.x+18;x<rect.x+rect.width-12;x+=55) {
      ctx.beginPath();
      ctx.moveTo(x,rect.y+rect.height);
      ctx.quadraticCurveTo(x+4,rect.y+36,x-5,rect.y+58);
      ctx.stroke();
    }
    return;
  }

  const side = style === "shrine" ? "#67654f" : (style === "arena" ? "#5b5c49" : p.stone);
  const top = style === "shrine" ? "#a59a72" : p.stoneTop;
  const grad = ctx.createLinearGradient(0,rect.y,0,rect.y+rect.height);
  grad.addColorStop(0,top);
  grad.addColorStop(0.38,side);
  grad.addColorStop(1,p.stoneDark);

  ctx.fillStyle = grad;
  ctx.fillRect(rect.x,rect.y,rect.width,rect.height);

  ctx.strokeStyle = "rgba(47,49,38,0.44)";
  ctx.lineWidth = 1;

  const bw = 38, bh = 16;
  for (let yy=rect.y+8;yy<rect.y+rect.height;yy+=bh) {
    const row = Math.floor((yy-rect.y)/bh);
    for (let xx=rect.x+(row%2?-19:0);xx<rect.x+rect.width;xx+=bw) {
      ctx.strokeRect(xx,yy,bw,bh);
    }
  }

  ctx.fillStyle = p.moss;
  for (let x=rect.x+5;x<rect.x+rect.width-4;x+=19) {
    const h = 3+Math.floor(seededNoise(x*.2+rect.y)*5);
    ctx.fillRect(x,rect.y-3,14,5);
    if (h>5) ctx.fillRect(x+4,rect.y+1,3,h);
  }

  if (style === "shrine") {
    ctx.strokeStyle = p.azulejo;
    ctx.lineWidth = 2;
    for (let x=rect.x+22;x<rect.x+rect.width-12;x+=44) {
      ctx.beginPath();
      ctx.moveTo(x,rect.y+7);
      ctx.lineTo(x+8,rect.y+13);
      ctx.lineTo(x+16,rect.y+7);
      ctx.lineTo(x+8,rect.y+2);
      ctx.closePath();
      ctx.stroke();
    }
  }
}

function drawRuinWall(rect) {
  drawStonePlatform(rect,"stone");
  if (rect.height > 130) {
    const p = AREA1_PALETTE;
    ctx.strokeStyle = p.azulejo;
    ctx.lineWidth = 2;
    const cy = rect.y+45;
    ctx.beginPath();
    ctx.moveTo(rect.x+10,cy);
    ctx.lineTo(rect.x+rect.width/2,cy-10);
    ctx.lineTo(rect.x+rect.width-10,cy);
    ctx.lineTo(rect.x+rect.width/2,cy+10);
    ctx.closePath();
    ctx.stroke();
  }
}

function drawWorldGeometry() {
  const data = currentAreaRoom();
  for (const seg of data.floors) drawGroundSegment(seg);
  for (const pit of data.pits) drawWaterPit(pit);
  for (const platform of platforms) drawStonePlatform(platform, platformStyleFor(platform));
  for (const wall of walls) drawRuinWall(wall);

  if (DEBUG_HITBOX) {
    ctx.save();
    ctx.strokeStyle="rgba(255,255,255,.28)";
    ctx.lineWidth=1;
    for (const s of solids) ctx.strokeRect(s.x,s.y,s.width,s.height);
    ctx.restore();
  }
}

function drawAnchor(anchor, reachable) {
  const p = AREA1_PALETTE;
  ctx.save();

  const isChain = anchor.kind === "chainroot";
  ctx.strokeStyle = isChain ? "#665f50" : (anchor.kind==="vine" ? p.vine : p.root);
  ctx.lineWidth = isChain ? 5 : (anchor.kind==="vine" ? 6 : 10);
  ctx.lineCap = "round";

  ctx.beginPath();
  ctx.moveTo(anchor.x-(anchor.kind==="root"?52:0),anchor.y-64);
  ctx.quadraticCurveTo(anchor.x-8,anchor.y-29,anchor.x,anchor.y);
  ctx.stroke();

  if (isChain) {
    ctx.strokeStyle = "#a09579";
    ctx.lineWidth = 3;
    for (let y=anchor.y-55;y<anchor.y-3;y+=10) {
      ctx.beginPath();
      ctx.ellipse(anchor.x,y,5,7,0,0,Math.PI*2);
      ctx.stroke();
    }
  }

  ctx.fillStyle = reachable ? "#d6e998" : "#83945f";
  ctx.beginPath();
  ctx.ellipse(anchor.x,anchor.y,11,8,-.2,0,Math.PI*2);
  ctx.fill();

  ctx.strokeStyle = reachable ? "#f1f7c2" : "#c2ce8a";
  ctx.lineWidth=2;
  ctx.stroke();

  if (reachable) {
    ctx.globalAlpha=.22;
    ctx.fillStyle="#eaffbb";
    ctx.beginPath();
    ctx.arc(anchor.x,anchor.y,22+Math.sin(performance.now()*.008)*3,0,Math.PI*2);
    ctx.fill();
  }
  ctx.restore();
}

drawGrapplePoint = function() {
  const origin = getTongueOrigin();
  for (const anchor of currentAreaRoom().anchors) {
    const d = distance(origin.x,origin.y,anchor.x,anchor.y);
    const dir = anchor.x>=origin.x?1:-1;
    const reachable = !grapple.active &&
      d<=anchor.grabRange &&
      (dir===mac.facing || d<=180) &&
      tongueLineClearToPoint(anchor.x,anchor.y);

    drawAnchor(anchor,reachable);
  }
};

function drawBromelia(x,y,s=1) {
  ctx.save();
  ctx.translate(x,y);
  for (let i=-3;i<=3;i++) {
    ctx.strokeStyle = i===0?"#c35543":"#527e46";
    ctx.lineWidth = 3.5*s;
    ctx.beginPath();
    ctx.moveTo(0,0);
    ctx.quadraticCurveTo(i*5*s,-14*s,i*10*s,-28*s-Math.abs(i)*2*s);
    ctx.stroke();
  }
  ctx.restore();
}

function drawLantern(x,y) {
  ctx.save();
  const glow = ctx.createRadialGradient(x,y,3,x,y,45);
  glow.addColorStop(0,"rgba(255,207,103,.32)");
  glow.addColorStop(1,"rgba(255,207,103,0)");
  ctx.fillStyle=glow;
  ctx.fillRect(x-48,y-48,96,96);
  ctx.fillStyle="#3e3327";
  ctx.fillRect(x-5,y-10,10,18);
  ctx.fillStyle="#ffd47b";
  ctx.fillRect(x-3,y-7,6,10);
  ctx.restore();
}

function drawMangroveWorldDecor() {
  ctx.save();
  ctx.strokeStyle=AREA1_PALETTE.root;
  ctx.lineCap="round";

  const roots = currentRoom===0 ? [95,1325,2540,3830,4710] : [130,1550,3220,4650];

  for (const rootX of roots) {
    ctx.lineWidth=20;
    ctx.beginPath();
    ctx.moveTo(rootX,ground.y+18);
    ctx.bezierCurveTo(rootX-65,ground.y-75,rootX+45,ground.y-180,rootX+18,ground.y-300);
    ctx.stroke();

    ctx.lineWidth=8;
    for (let i=0;i<3;i++) {
      ctx.beginPath();
      ctx.moveTo(rootX,ground.y-15-i*22);
      ctx.quadraticCurveTo(rootX+(i-1)*55,ground.y-45-i*12,rootX+(i-1)*100,ground.y);
      ctx.stroke();
    }
  }
  ctx.restore();

  const arches = currentRoom===0 ? [310,1460,2560,3890,4470] : [330,1570,2220,3300,4310];

  for (const x of arches) {
    ctx.fillStyle="rgba(91,92,72,.72)";
    ctx.fillRect(x,548,24,212);
    ctx.fillRect(x+112,565,24,195);

    ctx.strokeStyle="rgba(123,119,88,.82)";
    ctx.lineWidth=14;
    ctx.beginPath();
    ctx.arc(x+68,590,56,Math.PI,Math.PI*2);
    ctx.stroke();
  }

  const plants = currentRoom===0
    ? [[405,687],[705,677],[1530,612],[1775,528],[2620,688],[2950,522],[3330,343],[3910,688],[4290,545],[4525,618]]
    : [[350,688],[690,618],[1590,688],[1810,608],[2230,443],[3250,688],[3460,613],[4320,678]];

  for (const [x,y] of plants) drawBromelia(x,y,.9);

  if (currentRoom===0) {
    ctx.save();
    ctx.translate(3370,350);
    ctx.fillStyle="#6a6c55";
    ctx.fillRect(-18,-48,36,48);
    ctx.beginPath();
    ctx.arc(0,-50,23,0,Math.PI*2);
    ctx.fill();
    ctx.fillStyle="#b9b27b";
    ctx.beginPath();
    ctx.arc(-8,-55,3,0,Math.PI*2);
    ctx.arc(8,-55,3,0,Math.PI*2);
    ctx.fill();
    ctx.restore();

    drawLantern(3275,325);
    drawLantern(3465,325);
  } else {
    drawLantern(2140,430);
    drawLantern(2380,430);
    drawLantern(4140,610);
    drawLantern(4660,610);
  }
}

function drawForeground() {
  ctx.save();
  ctx.translate(-Math.round(camera.x*.045),0);
  ctx.globalAlpha=.62;
  ctx.strokeStyle="#26362d";
  ctx.lineCap="round";

  const clusters = [40,620,1280,1980,2670,3460,4260,4930];

  for (const x of clusters) {
    ctx.lineWidth=14;
    ctx.beginPath();
    ctx.moveTo(x,HEIGHT+20);
    ctx.quadraticCurveTo(x-35,HEIGHT-95,x+15,HEIGHT-180);
    ctx.stroke();

    ctx.lineWidth=6;
    for (let i=-2;i<=2;i++) {
      ctx.beginPath();
      ctx.moveTo(x,HEIGHT-65);
      ctx.quadraticCurveTo(x+i*20,HEIGHT-110,x+i*42,HEIGHT-128);
      ctx.stroke();
    }
  }
  ctx.restore();

  ctx.save();
  const v=ctx.createRadialGradient(WIDTH/2,HEIGHT/2,HEIGHT*.30,WIDTH/2,HEIGHT/2,WIDTH*.75);
  v.addColorStop(0,"rgba(0,0,0,0)");
  v.addColorStop(1,"rgba(12,25,20,.26)");
  ctx.fillStyle=v;
  ctx.fillRect(0,0,WIDTH,HEIGHT);
  ctx.restore();
}

drawAnimatedFly = function(fly, type) {
  if (!fly.alive) return;
  const water = type === "waterFly" || type === "water";
  const t = fly.animTime || 0;
  const flap = Math.abs(Math.sin(t*18));

  ctx.save();
  ctx.translate(fly.x,fly.y);

  const glow=ctx.createRadialGradient(0,0,2,0,0,26);
  glow.addColorStop(0, water?"rgba(92,218,255,.34)":"rgba(239,250,255,.30)");
  glow.addColorStop(1,"rgba(255,255,255,0)");
  ctx.fillStyle=glow;
  ctx.fillRect(-30,-30,60,60);

  ctx.fillStyle=water?"rgba(200,241,255,.86)":"rgba(247,247,255,.86)";
  ctx.beginPath();
  ctx.ellipse(-6,-4,8,3+flap*4,-.55,0,Math.PI*2);
  ctx.ellipse(6,-4,8,3+flap*4,.55,0,Math.PI*2);
  ctx.fill();

  ctx.fillStyle=water?"#3cc6f1":"#e8eef7";
  ctx.beginPath();
  ctx.ellipse(0,1,8,7,0,0,Math.PI*2);
  ctx.fill();

  ctx.fillStyle=water?"#dffaff":"#fff4c9";
  ctx.beginPath();
  ctx.arc(-2,-1,1.5,0,Math.PI*2);
  ctx.arc(2,-1,1.5,0,Math.PI*2);
  ctx.fill();

  ctx.restore();
};

function drawMangueEnemy(e) {
  if (!e.alive) return;

  const t = e.animTime || 0;
  const hurt = e.hurtTimer > 0;
  const cx=e.x+e.width/2;
  const cy=e.y+e.height/2;

  ctx.save();
  ctx.lineJoin="round";
  ctx.lineCap="round";

  if (e.type === "caranguejo") {
    const step=Math.sin(t*11)*3;

    ctx.strokeStyle="#5c3027";
    ctx.lineWidth=5;

    for (const d of [-1,1]) {
      ctx.beginPath();
      ctx.moveTo(cx+d*13,cy+7);
      ctx.lineTo(cx+d*(25+step),cy+16);
      ctx.lineTo(cx+d*32,cy+11);
      ctx.stroke();

      ctx.beginPath();
      ctx.moveTo(cx+d*12,cy+1);
      ctx.lineTo(cx+d*(25-step),cy-7);
      ctx.stroke();
    }

    ctx.fillStyle=hurt?"#f2aaa0":"#b95d3e";
    ctx.strokeStyle="#71382c";
    ctx.lineWidth=2.5;
    ctx.beginPath();
    ctx.ellipse(cx,cy+5,e.width*.39,e.height*.29,0,0,Math.PI*2);
    ctx.fill();
    ctx.stroke();

    ctx.fillStyle="#e7865e";
    ctx.beginPath();
    ctx.ellipse(cx,cy+1,e.width*.25,e.height*.16,0,0,Math.PI*2);
    ctx.fill();

    ctx.fillStyle="#f2d9a1";
    ctx.beginPath();
    ctx.arc(cx-8,cy-7,3,0,Math.PI*2);
    ctx.arc(cx+8,cy-7,3,0,Math.PI*2);
    ctx.fill();

    ctx.fillStyle="#22271f";
    ctx.beginPath();
    ctx.arc(cx-8,cy-7,1.2,0,Math.PI*2);
    ctx.arc(cx+8,cy-7,1.2,0,Math.PI*2);
    ctx.fill();
  } else {
    const isPerereca=e.type==="perereca";
    const body = isPerereca ? "#5e8a4c" : "#7c7245";
    const light = isPerereca ? "#8dbb63" : "#9a8e55";
    const outline = isPerereca ? "#355537" : "#514a32";
    const squash = isPerereca ? Math.sin(t*6)*2 : Math.sin(t*3)*1.2;

    ctx.strokeStyle=outline;
    ctx.lineWidth=4;
    ctx.beginPath();
    ctx.moveTo(cx-13,cy+13);
    ctx.lineTo(cx-23,cy+20);
    ctx.moveTo(cx+13,cy+13);
    ctx.lineTo(cx+23,cy+20);
    ctx.stroke();

    ctx.fillStyle=hurt?"#e6aaa2":body;
    ctx.strokeStyle=outline;
    ctx.lineWidth=2.5;
    ctx.beginPath();
    ctx.ellipse(cx,cy+6,e.width*.45,e.height*.34+squash,0,0,Math.PI*2);
    ctx.fill();
    ctx.stroke();

    ctx.fillStyle=hurt?"#f4c2b9":light;
    ctx.beginPath();
    ctx.ellipse(cx,e.y+14,e.width*.33,14,0,0,Math.PI*2);
    ctx.fill();

    ctx.fillStyle="#eadb9e";
    ctx.beginPath();
    ctx.arc(cx-9,e.y+8,4,0,Math.PI*2);
    ctx.arc(cx+9,e.y+8,4,0,Math.PI*2);
    ctx.fill();

    ctx.fillStyle="#1d261d";
    ctx.beginPath();
    ctx.arc(cx-9,e.y+8,1.8,0,Math.PI*2);
    ctx.arc(cx+9,e.y+8,1.8,0,Math.PI*2);
    ctx.fill();
  }

  const ratio=Math.max(0,e.hp/e.maxHp);
  ctx.fillStyle="rgba(27,35,28,.78)";
  ctx.fillRect(e.x,e.y-10,e.width,4);
  ctx.fillStyle=e.type==="caranguejo"?"#e8825d":(e.type==="perereca"?"#87b75e":"#cfb65f");
  ctx.fillRect(e.x,e.y-10,e.width*ratio,4);

  if (DEBUG_HITBOX) {
    ctx.strokeStyle="#ffe082";
    ctx.lineWidth=1;
    ctx.strokeRect(e.x,e.y,e.width,e.height);
  }
  ctx.restore();
}

drawEnemy = function() {
  if (enemy.alive) drawMangueEnemy(enemy);
  for (const e of getExtraEnemies()) drawMangueEnemy(e);
};

drawCheckpoint = function() {
  const cp=getCheckpointForCurrentRoom();
  if (!cp) return;

  const active=currentCheckpointId===cp.id;
  const cx=cp.x+cp.width/2;
  const base=cp.y+cp.height;

  ctx.save();

  const glow=ctx.createRadialGradient(cx,cp.y+24,4,cx,cp.y+24,55);
  glow.addColorStop(0,active?"rgba(105,255,177,.28)":"rgba(255,213,100,.22)");
  glow.addColorStop(1,"rgba(255,255,255,0)");
  ctx.fillStyle=glow;
  ctx.fillRect(cx-60,cp.y-30,120,120);

  ctx.fillStyle="#555846";
  ctx.fillRect(cx-16,base-38,32,38);
  ctx.beginPath();
  ctx.arc(cx,base-42,20,0,Math.PI*2);
  ctx.fill();

  ctx.fillStyle=active?"#8ef0ad":"#e4c86b";
  ctx.beginPath();
  ctx.arc(cx-7,base-47,3,0,Math.PI*2);
  ctx.arc(cx+7,base-47,3,0,Math.PI*2);
  ctx.fill();

  ctx.restore();
};

drawRoomExit = function() {
  const x=currentRoom===0?WORLD_WIDTH-38:0;

  ctx.save();
  ctx.fillStyle="rgba(78,84,65,.85)";
  ctx.fillRect(x,ground.y-135,38,135);

  ctx.strokeStyle="#8f9274";
  ctx.lineWidth=6;
  ctx.beginPath();
  ctx.arc(x+19,ground.y-128,28,Math.PI,Math.PI*2);
  ctx.stroke();

  ctx.fillStyle="rgba(171,230,212,.18)";
  ctx.fillRect(x,ground.y-112,38,112);
  ctx.restore();
};

draw = function() {
  ctx.clearRect(0,0,WIDTH,HEIGHT);
  drawMangroveBackground();

  ctx.save();
  ctx.translate(-Math.round(camera.x),-Math.round(camera.y));

  drawMangroveWorldDecor();
  drawWorldGeometry();
  drawBossGate();
  drawRoomExit();
  drawCheckpoint();
  drawGrapplePoint();
  drawWaterFly();
  drawWindFly();
  drawGrappleRope();
  drawTongue();
  drawNormalAttack();
  drawWaterShots();
  drawWaterBurst();
  drawWaterUltimate();
  drawWindBlades();
  drawWindGust();
  drawWindUltimate();
  drawEnemy();
  drawBoss();
  drawMacGroundShadow();

  if (!mac.dead) {
    const blink=mac.invulnerableTimer>0 && Math.floor(mac.invulnerableTimer*18)%2===0;
    if (!blink) drawMacSprite();
    drawMacHitbox();
  }

  drawTongueHitbox();
  ctx.restore();

  drawForeground();
  drawHUD();
};
